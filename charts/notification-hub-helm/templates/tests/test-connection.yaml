apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notification-hub.fullname" . }}-test-connection"
  labels:
    {{- include "notification-hub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notification-hub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
